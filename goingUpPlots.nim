# going up
import ggplotnim,
       sequtils,
       seqmath,
       math,
       streams,
       main

proc plotSpeed(sphere): seq[DataPoint] =
  return nil

proc plotSpeed(sphere) =
  return nil

proc plotSpeed(sphere) =
  return nil

proc velocityGoingUp(x: float): float =
  let v = sqrt()
